import { ImageSourcePropType } from 'react-native';

export const BIRDR_FLOCK_IMAGES = {
  invite: require('../../assets/birdr-flock-invite.png') as ImageSourcePropType,
  leaderboard: require('../../assets/birdr-leaderboard.png') as ImageSourcePropType,
};
