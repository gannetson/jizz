"""Offline machine first-assertion (image approve vs reject) — features, train, predict."""

# Default features version to use when not explicitly specified.
DEFAULT_FEATURES_VERSION = 'handcrafted_v2'

# Backwards-compatible alias (older artifacts refer to this key).
FEATURES_VERSION = DEFAULT_FEATURES_VERSION
